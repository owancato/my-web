version: '3.8'

services:
  vue-dev:
    build: .
    volumes:
      - .:/app
    ports:
      - "5173:5173"
      - "8080:8080"
    command: ["npm", "run", "dev"]

  npm-cli:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: ["npm"]
